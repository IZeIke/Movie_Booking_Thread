File Movie.java
	
	มี class Movie เพื่อจะใช้แทนหนังแต่ละเรื่อง 
	โดยใน class นี้ จะมีตัวแปร TicketNumber ตามโจทย์กำหนดคือ 20 และ 
	function reserve เพื่อจองตั๋ว ตามจำนวนตัวแปร n และ
	function getTicketNumber เป็น function ที่จะส่งจำนวนตั๋วที่เหลือออกมา

File TicketBooking.java
	
	สร้างตัวแปร m1 - m5 เป็น Movie เพื่อแทนหนังทั้ง 5 เรื่อง
	ให้ user1 - user5 แทน user โดยให้ user เป็น Thread เพื่อให้เกิดการคำนวณพร้อมกันได้ 
	ให้ class User extends มาจาก Thread 
	ใน User จะมี username และ รายชื่อหนัง 
	มีการประกาศ ตัวแปรชนิด Random ชื่อ randMovie เพื่อจะใช้สุ่มเลือกว่าจะจองหนังเรื่องใด
	มีการประกาศ ตัวแปรชนิด Random ชื่อ randTicket เพื่อสุ่มจำนวนตั๋วที่จะจอง
	ตัวแปร decision เพื่อเลือกว่าจะจองหรือไม่จอง
	ใน function run จะเป็นการทำงานทั้งหมด โดยเริ่มจาก random ว่าจะจองหนังเรื่องใด และ random ว่าจะซื้อตั๋วกี่ใบ 
	และทำการหักลบตั๋วหนังของเรื่องนั้นที่เหลืออยู่ทันที
	จากนั้น sleep 1000 millisecond ต่อมาให้ random ตัดสินใจว่าจะซื้อตั๋วหรือไม่
	ถ้าไม่ซื้อ จะทำการบวกตั๋วที่หักไป ให้เท่าเดิม และแสดงข้อความ
	ถ้าซื้อ จะทำการเช็คก่อนว่า สามารถจองตั๋วจำนวนเท่านั้นได้หรือไม่ ถ้าจำนวนตั๋วน้อยกว่า 0 แปลว่าจองไม่ได้และจะถือว่าเป็นการจองที่ซ้ำกันและแสดงข้อความออกมา ถ้าจองได้จะแสดงข้อความออกมา 
	สุดท้ายจะแสดงว่า ตอนนี้หนังเรื่องที่ user คนนั้นจองเหลือตั๋วกี่ใบ

จากการทำให้ User แต่ละคนเป็น Thread จะส่งผลให้ เราสามารถ จองตั๋วในเรื่องนั้นๆได้ พร้อมๆกัน 
